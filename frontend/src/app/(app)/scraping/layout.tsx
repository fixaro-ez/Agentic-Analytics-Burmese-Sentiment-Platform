import type { Metadata } from "next"

export const metadata: Metadata = { title: "Scraping" }

export default function ScrapingLayout({ children }: { children: React.ReactNode }) {
  return children
}
