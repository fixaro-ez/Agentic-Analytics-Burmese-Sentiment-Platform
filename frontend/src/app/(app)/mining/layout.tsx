import type { Metadata } from "next"

export const metadata: Metadata = { title: "Feedback Patterns" }

export default function MiningLayout({ children }: { children: React.ReactNode }) {
  return children
}
